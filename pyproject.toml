[project]
name = "whispercpp-benchmark"
version = "0.1.0"
description = "Benchmark pywhispercpp inference speed across multiple runs"
requires-python = ">=3.14"
dependencies = [
    "miniaudio>=1.2",
    "pywhispercpp>=1.4.1",
]
